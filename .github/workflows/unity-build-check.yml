name: Unity Build (Buildalon)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true

      - uses: buildalon/unity-setup@v2
        with:
          build-targets: 'StandaloneLinux64'

      - uses: buildalon/activate-unity-license@v2
        with:
          license: 'Personal'
          username: '${{ secrets.UNITY_USERNAME }}'
          password: '${{ secrets.UNITY_PASSWORD }}'

      - name: Install Buildalon Package
        run: |
          npm install -g openupm-cli
          openupm add com.virtualmaker.buildalon

      - uses: buildalon/unity-action@v3
        name: Build Linux
        with:
          build-target: 'StandaloneLinux64'
          args: '-quit -batchmode -executeMethod Buildalon.Editor.BuildPipeline.UnityPlayerBuildTools.StartCommandLineBuild'
