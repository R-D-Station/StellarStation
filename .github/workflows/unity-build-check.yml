name: Unity Build Check (Windows & Ubuntu)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: Build ${{ matrix.targetPlatform }} on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
      pull-requests: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            targetPlatform: StandaloneWindows64
          - os: ubuntu-latest
            targetPlatform: StandaloneLinux64
    steps:
      - name: Log start info
        shell: bash
        run: |
          echo "Starting build process..."
          echo "Runner OS: ${{ matrix.os }}"
          echo "Target platform: ${{ matrix.targetPlatform }}"

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Cache Unity Library
        uses: actions/cache@v4
        with:
          path: Library
          key: Library-${{ runner.os }}-${{ hashFiles('Assets/**', 'Packages/**', 'ProjectSettings/**') }}
          restore-keys: |
            Library-${{ runner.os }}-
            Library-

      - name: Debug secrets
        shell: bash
        run: |
          if [ -z "${{ secrets.UNITY_LICENSE }}" ]; then
            echo "ERROR: UNITY_LICENSE secret is empty!"
          else
            echo "UNITY_LICENSE length: $(echo '${{ secrets.UNITY_LICENSE }}' | wc -c)"
          fi

      - name: Build Unity Project
        uses: game-ci/unity-builder@v4
        env:
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
        with:
          unityVersion: 6000.3.11f1
          targetPlatform: ${{ matrix.targetPlatform }}

      - name: Set build status label on PR
        if: github.event_name == 'pull_request' && always()
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          REPO: ${{ github.repository }}
          JOB_STATUS: ${{ job.status }}
        run: |
          if [ "$JOB_STATUS" = "failure" ]; then
            echo "Build failed. Adding 'Do not merge' label..."
            gh pr edit $PR_NUMBER --add-label "Do not merge"
          elif [ "$JOB_STATUS" = "success" ]; then
            echo "Build succeeded. Removing 'Do not merge' label..."
            gh pr edit $PR_NUMBER --remove-label "Do not merge" || true
          else
            echo "Build status is '$JOB_STATUS'. No label changes."
          fi
